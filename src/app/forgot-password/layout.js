
export const metadata = {
  title: "Forgot Password - FloodWatch",
};

export default function ForgotPasswordLayout({ children }) {
  return (
    <div className="h-screen flex flex-col items-center justify-center">
      {children}
    </div>
  );
}
